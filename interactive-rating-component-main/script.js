const oneEl = document.querySelector("one");
const twoEl = document.querySelector("two");
const threeEl = document.querySelector("three");
const fourEl = document.querySelector("four");
const fiveEl = document.querySelector("five");

oneEl.addEventListener('click', () => {
    oneEl.classList.add('mainbox__rating-numbers--selected');
});
// twoEl.addEventListener('click', select());
// threeEl.addEventListener('click', select());
// fourEl.addEventListener('click', select());
// fiveEl.addEventListener('click', select());


